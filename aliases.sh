#!/bin/bash

# You could run this command and connect directly to "chess_api" database appending db name at the end:
# $ chesspql chess_api
alias chesspsql="docker exec -it chess_postgresql psql -U postgres"
